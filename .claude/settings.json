{
  "permissions": {
    "defaultMode": "bypassPermissions"
  },
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/maksimrazantsau/weather_vs_taxi_trips && if [ -n \"$(git status --porcelain)\" ]; then git add -A && git commit -m \"auto: $(git status --porcelain | awk '{print $2}' | sed 's|.*/||' | sort -u | head -5 | tr '\\n' ' ' | xargs)\" && git push; fi",
            "timeout": 30,
            "statusMessage": "Committing & pushing..."
          }
        ]
      }
    ]
  }
}
