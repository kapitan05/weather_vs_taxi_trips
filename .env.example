# Copy to .env and fill in values for production
POSTGRES_USER=data_engineer
POSTGRES_PASSWORD=admin
POSTGRES_DB=nyc_weather_taxi

# Used by ingest modules (defaults to dev values if unset)
DB_URL=jdbc:postgresql://postgres-dwh:5432/nyc_weather_taxi
DB_USER=data_engineer
DB_PASSWORD=admin
